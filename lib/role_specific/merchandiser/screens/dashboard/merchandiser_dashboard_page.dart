import 'package:flutter/material.dart';

class MerchandiserDashboardPage extends StatelessWidget {
  const MerchandiserDashboardPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Merchandiser Dashboard')),
      body: const Center(child: Text('Welcome to Merchandiser Dashboard')),
    );
  }
}
